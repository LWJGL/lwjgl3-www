{
  "name": "cloudfront-viewer-request",
  "description": "Handles redirect @ the edge, optimizes caching headers and forwards request to origin.",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@types/aws-lambda": "8.10.69",
    "@types/node": "14.14.20"
  },
  "scripts": {
    "build": "rm -r dist/*; tsc && copyfiles package.json ./dist/",
    "ncu": "npx npm-check-updates -i"
  }
}
